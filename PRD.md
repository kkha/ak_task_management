# AK Task Management — PRD

- **문서 버전**: 4.0
- **작성일**: 2026-09-08
- **작성자**: kkha123@gmail.com
- **상태**: 구현됨 (v4.0 — 세부분류, 작업중 상태, 우선순위, 상세메모, 여러 날, 반복,
  완료 히스토리, 전체 폭 3단 레이아웃, 실행 방식 전환. v3.3 = 필터 개수·통합 목록·주간/월간 계획.
  v3.x = 2-패널·달력·주/월·메모·배포본. v2 = UI 개편·정렬·DnD·내보내기/가져오기)
- 전체 버전별 변경 이력은 [`CHANGELOG.md`](CHANGELOG.md).

---

## 1. 개요

### 1.1 배경
매일 10~20개의 할 일을 처리하는 개인 사용자가, 별도 계정 생성이나 설치 없이
브라우저를 열자마자 바로 쓸 수 있는 가벼운 할 일 관리 도구가 필요하다.
기존 서비스(Todoist, Notion 등)는 기능이 많고 무거우며, 로그인·동기화 등
불필요한 절차가 진입 장벽이 된다.

### 1.2 목표
- 브라우저에서 즉시 실행되는 단일 페이지 앱
- 새로고침·재방문 후에도 데이터 유지
- 추가/수정/삭제/완료 체크를 3초 안에 끝낼 수 있는 조작성
- 카테고리별 분류와 진행률 확인
- 할 일을 **날짜에 배치**해 일별/주별/월별로 계획하고, 지난 날짜의 기록을 확인
- **전체 폭 3단** — 좌측 카테고리·세부분류 트리 / 중앙 달력·메모 / 우측 상세 할 일 관리
- 달력에 **주 번호(WK37)** 표시
- 상태(대기/작업중/완료) · 우선순위 · 상세메모 · 여러 날 · 반복 일정 · 완료 히스토리
- 회사 PC에서 `index.html` 을 열고 전용 아이콘 바로가기를 작업 표시줄에 고정

### 1.3 범위 밖 (Non-Goals)
- 다중 사용자, 로그인, 클라우드 동기화
- 여러 기기 간 데이터 공유
- 마감일 알림, 반복 일정, 하위 작업(subtask)
- 팀 협업, 공유, 코멘트
- 백엔드 서버, 데이터베이스

---

## 2. 사용자 및 시나리오

### 2.1 대상 사용자
개인 용도로 하루치 할 일을 관리하는 1인 사용자. 기술 지식은 불필요.

### 2.2 핵심 시나리오
| # | 상황 | 사용자 행동 | 기대 결과 |
|---|------|-------------|-----------|
| S1 | 아침에 오늘 할 일을 정리한다 | 입력창에 할 일을 적고 카테고리 선택 후 추가 | 목록에 항목이 즉시 나타남 |
| S2 | 일을 하나 끝냈다 | 항목의 체크박스를 클릭 | 완료 표시(취소선), 진행률 상승 |
| S3 | 할 일 내용을 잘못 적었다 | 항목을 더블클릭(또는 수정 버튼)해 텍스트 편집 | 수정 내용이 저장됨 |
| S4 | 필요 없어진 할 일이 있다 | 삭제 버튼 클릭 | 목록에서 제거, 진행률 재계산 |
| S5 | 업무 관련 할 일만 보고 싶다 | 카테고리 필터에서 "업무" 선택 | 업무 항목만 표시 |
| S6 | 브라우저를 껐다 다시 켠다 | 앱 페이지 재방문 | 이전 데이터가 그대로 보임 |

---

## 3. 기능 요구사항

### 3.1 할 일 추가
- 텍스트 입력창 + 카테고리 선택(드롭다운) + 추가 버튼
- Enter 키로도 추가 가능
- 빈 문자열 / 공백만 입력 시 추가되지 않음
- 신규 항목은 목록 상단(또는 하단 — 구현 시 결정)에 추가되며 기본 상태는 "미완료"
- 항목 데이터: `id`, `text`, `category`, `completed`, `createdAt`

### 3.2 할 일 수정
- 항목 텍스트를 인라인 편집(더블클릭 또는 수정 아이콘)
- 카테고리도 변경 가능
- 빈 내용으로 저장 시 수정 취소(또는 삭제 확인 — 구현 시 결정)
- Esc로 편집 취소, Enter로 저장

### 3.3 할 일 삭제
- 각 항목에 삭제 버튼
- 삭제 시 즉시 제거 (확인 다이얼로그는 선택 사항, 기본 없음)

### 3.4 완료 체크
- 각 항목에 체크박스
- 체크 시 완료 처리: 취소선 등 시각적 구분
- 다시 클릭하면 미완료로 토글
- 완료 여부가 진행률에 반영됨

### 3.5 카테고리 분류
- 고정 카테고리 3종: **개인 / 업무 / 공부**
- 각 항목은 반드시 하나의 카테고리에 속함
- 카테고리별 색상 구분(뱃지 또는 좌측 색상 바)
- 필터: 전체 / 개인 / 업무 / 공부 중 선택해 목록 필터링
- 각 필터 칩에 **현재 보기 기준 등록 개수**를 표기: `전체(10) 개인(2) 업무(8) 공부(0)`
  (기간 스코프만 적용, 완료 숨김·현재 필터와 무관)

### 3.6 진행률 보기
- 완료 항목 비율(%)을 숫자 + 진행 바로 표시. 예: "7 / 15 완료 (47%)"
- **현재 보기 기준**으로 계산: 일별=그 날, 주별=그 주, 월별=그 달의 할 일만. (필터는 무관)
- 항목이 0개일 때: "할 일이 없습니다" 안내, 진행 바 0%

### 3.7 2-패널 레이아웃 · 달력 · 일/주/월 보기 · 히스토리 (v3)

**레이아웃** (구글 캘린더식): 좌측 = 달력(내비게이터), 우측 = 선택 기간의 상세 할 일 관리.
좁은 화면(≤880px)에서는 달력이 위로 스택.

**달력** (좌측 패널):
- 항상 일요일 시작. 좌측에 **주 번호(WK)** 열 — 일요일 기준 연중 주차(1/1 포함 주 = 1주차),
  숫자는 보라색으로 강조.
- 오늘 강조, 할 일 있는 날은 점 표시(모두 완료 시 초록 점).
- `‹ ›`로 다른 달 넘겨보기(월별 보기 외에는 선택과 독립적인 미리보기).
- **클릭 동작은 현재 보기에 따라 다르다**:
  - 일별: 날짜 클릭 → 그 날로 이동
  - 주별: **WK 열의 주 번호만 클릭 가능** → 그 주로 이동. 개별 날짜 셀은 클릭 불가.
    선택한 주 한 줄 전체가 파란 배경.
  - 월별: 표시 중인 달 전체가 파란 배경. 날짜 클릭 시 그 날이 속한 달로.

**보기 단위** (우측 상단 토글, 설정 저장됨). 셋 다 **하나의 통합 목록**으로 관리한다
(날짜별 그룹/아젠다 없음):
- **일별**(기본): 선택한 하루의 할 일.
- **주별**: 그 주(일~토) 전체를 한 목록으로. 라벨 "WK37 · 9월 6일 – 9월 12일".
- **월별**: 그 달 전체를 한 목록으로. 라벨 "2026년 9월".
- 주·월 보기에서는 각 행에 뱃지: 특정 날 할 일 → 날짜("오늘" / "9/8 (화)"),
  주 계획 → `WK37`(남색 필), 월 계획 → `9월`(분홍 필) — 색으로 구분.
  `정렬 → 날짜순`으로 정리. 특정 날만 보려면 달력에서 클릭.

**주간 / 월간 계획** (`Task.scope`): 특정 날이 아니라 "그 주 안에" / "그 달 안에" 할 일도 있다.
- **주별 보기에서 추가** → `scope: "week"` — 그 주 통합 목록에만 표시(일별에는 안 뜸)
- **월별 보기에서 추가** → `scope: "month"` — 그 달 통합 목록에만 표시(일별·주별에는 안 뜸)
- 계획 항목은 통합 목록 **맨 위**에 모이고, 달력 점 표시에는 포함되지 않는다
- `scope` 없음(예전 데이터·일별 추가) = `"day"` (기존 동작 그대로)

**기간 이동**: `‹ ›`로 하루/한 주/한 달씩, "오늘" 버튼으로 복귀. 지난 기간으로 이동 = 히스토리 열람.

**새 할 일 등록**: 일별 = `selectedDate`의 그 날 할 일. 주·월별 = 그 주/달의 계획.
composer 아래에 "＋ 이번 주(WK37) 계획으로 추가됩니다" / "＋ 2026년 9월 계획으로 추가됩니다" 안내.

**선택 날짜는 저장하지 않는다** — 새로고침하면 오늘로 복귀(일/주/월 보기 설정은 유지).

### 3.9 메모 스티커 (v3.2)
- 좌측 패널 달력 아래에 자유 입력 `<textarea>`. 스티키 노트 스타일(연한 노랑).
- 입력이 멈추면(약 0.4초) 자동 저장(`task-app.memo`), "저장됨" 잠깐 표시.
- 날짜/보기와 무관한 단일 메모(하나만). 세로 크기 조절 가능.

### 3.10 실행 · 배포본 (v4.0에서 방식 전환)
- `npm run package` → `release/AK Task Management/` (`index.html` + `app-icon.ico` +
  `AK Task Management.lnk` + `바로가기 만들기.cmd` + `변경이력.txt` + `사용법.txt`) + zip.
- **`index.html` 더블클릭 = 기본 브라우저에서 일반 탭으로 열림.** 설치·계정·네트워크 불필요.
- 작업 표시줄 고정: `바로가기 만들기.cmd` 로 그 PC 경로에 맞는 `.lnk` 생성 → 우클릭 → "추가 옵션 표시"
  → "작업 표시줄에 고정". `.lnk` 대상은 `explorer.exe` + `index.html` 인수(진짜 실행 파일이라
  고정 메뉴가 뜨고, 브라우저 종류에 의존하지 않음). 아이콘은 `app-icon.ico`(전용 체크 마크).
- v3.2의 `--app` 모드 전용 창·전용 브라우저 프로필·고정 창 크기는 폐기.

### 3.8 데이터 영속성
- 브라우저 `localStorage`에 전체 할 일 목록을 JSON으로 저장
- 추가/수정/삭제/체크 등 모든 변경 시점에 저장
- 앱 로드 시 `localStorage`에서 복원, 없으면 빈 목록으로 시작
- 저장 키: 예) `task-app.tasks`
- 데이터 파싱 실패 시 빈 목록으로 폴백(앱이 깨지지 않도록)

---

## 4. 비기능 요구사항

| 항목 | 요구사항 |
|------|----------|
| 실행 환경 | 최신 데스크톱 브라우저(Chrome, Edge, Firefox, Safari). `dist/index.html` 더블클릭 또는 정적 호스팅으로 실행 |
| 기술 스택(v2) | 순수 HTML + CSS + Vanilla JS (ES 모듈). 런타임 의존성·프레임워크 없음. 개발용으로만 Vite/Vitest/ESLint |
| 파일 구성(v2) | `index.html` + `src/*.js` (ES 모듈) + `src/styles/*.css`. 빌드 시 단일 `dist/index.html` 로 인라인 |
| 성능 | 항목 50개 기준 조작 지연 체감 없음 |
| 반응형 | 데스크톱 우선, 모바일 브라우저에서도 목록이 세로로 정상 표시 |
| 접근성 | 체크박스·버튼에 키보드 접근 가능, 버튼에 aria-label |
| 데이터 크기 | localStorage 한도(약 5MB) 내에서 충분. 초과 대비는 범위 밖 |
| 오프라인 | 네트워크 불필요, 완전 오프라인 동작 |

---

## 5. 화면 구성 (개략)

전체 폭 3단. app-bar는 전폭, 그 아래 `.layout` 그리드
(`minmax(180,220) minmax(240,290) 1fr`):

```
┌──────────────────────────────────────────────────────────────────────┐
│  AK Task Management v4.0.0          [테마][히스토리][분류설정][내보내기][가져오기]│
├─────────────┬──────────────────┬─────────────────────────────────────┤
│ 분류  [편집] │ ‹ 2026년 9월 ›    │ [오늘] ‹ ›  오늘·9월 8일(화)   [일|주|월]│
│ 전체     12 │ WK│일 월 …토      │ 진행률 ▓▓░░  1/8 완료 (13%)          │
│ 개인      3 │ 36│ .  1 … 5      │ [ 할 일 입력…] [개인▾][세부▾][우선▾][반복▾][추가]│
│  운동     1 │ 37│ 7  8•…12      │ ── 업무 › 빅데이터  ✕      완료숨기기 정렬▾│
│ 업무      6 │ 38│13 14 …        │ ⠿ ☐ [진행] ▲ 거래처 미팅 자료  [UT일반][업무] 수정 삭제│
│  빅데이터 2 │ • 할 일 있는 날   │      12시 발표 · 최종본 확인 …(메모)      │
│  AI/자동화1 │ [메모 스티커]     │ ⠿ ☐ [진행]  데이터 파이프라인 점검  [빅데이터][업무] …│  ← 작업중=회색
│  미분류   1 │                  │ ⠿ ☐ [진행] 🔁 스탠드업 미팅  [UT일반][업무] …│
│ 공부      1 │                  │ ⠿ ☐ [진행] ▲ UT 세션 (2일)  [1일차][UT과제][업무] …│
└─────────────┴──────────────────┴─────────────────────────────────────┘
(≤1180px: 분류 트리는 "☰ 분류" 슬라이드오버. ≤820px: 세로 스택)
```

편집 카드(수정 클릭 시 그 행이 3줄 카드로):
```
[ 할 일 내용 .......................................................... ]
[ 카테고리▾ ] [ 세부분류▾ ] [ 우선순위▾ ]
[ 일별▾ ] [ 2026-09-08 ] ~ [ 2026-09-09 ]                    [저장] [취소]
[ 상세 내용 (선택) .................................................... ]
```

완료 히스토리(다이얼로그): [검색…] [완료일 ~ ]  · 완료일별 그룹 · "14:30 · 항목 · 소요 3시간 · 메모"

---

## 6. 데이터 모델

```js
// localStorage["task-app.tasks"] = JSON.stringify(Task[])
// 필수 6개 + 선택 필드. 선택 필드는 "기본값이 아닐 때만" 저장한다(레코드를 가볍게).
Task = {
  id: string,
  text: string,
  category: "개인" | "업무" | "공부",
  completed: boolean,           // done 의 정본 (frozen)
  createdAt: number,            // Date.now() — 생성 시각
  date: string,                 // "YYYY-MM-DD" — 속한 날 / 주·월은 그 안의 기준일

  scope?:    "week" | "month",  // 그 주/달 계획. 없으면 "day"           (v3.3)
  status?:   "doing",           // "작업중". done=completed, 그 외 todo   (v4)
  priority?: "high" | "low",    // 없으면 "normal"                        (v4)
  subcategory?: string,         // 그 카테고리의 task-app.subcategories 중 (v4)
  endDate?:  string,            // 여러 날 종료일(포함). date 보다 클 때만  (v4)
  notes?:    string,            // 상세메모                              (v4)
  doneAt?:   number,            // 완료 시각(ms). completed일 때만        (v4)
  recurrence?: { freq: "daily"|"weekdays"|"weekly", days?: number[], until?: string },
  series?:   string,            // 같은 반복에서 생성된 인스턴스 묶음 id  (v4)
}
// 구 데이터: date 없으면 createdAt에서 보정. 모르는·모순된 선택 필드는 떼고 항목은 살린다.
// 내보내기 JSON 스키마 version = 5.

// localStorage["task-app.subcategories"] = { 개인: [], 업무: [...], 공부: [] }   (v4)
//   첫 실행 시 업무만 시딩(빅데이터·AI/자동화·UT일반·UT과제).

// localStorage["task-app.prefs"] = { filter, sort, hideCompleted, theme, view }
//   filter: "전체" | "<카테고리>" | "<카테고리>/<세부분류>" | "<카테고리>/"(미분류)  (v4)
//   sort:  ... | "priority" (v4) | "date-asc" (v3.3) | ...
//   view:  "day" | "week" | "month"
```

앱 상태(런타임, 요약):
```js
state = {
  tasks: Task[],
  subcats: { 개인, 업무, 공부: string[] },  // task-app.subcategories 사본
  editingId: string | null,
  expandedNotes: Set<id>,        // 표시 목록에서 펼친 메모 (저장 안 함)
  selectedDate: "YYYY-MM-DD",    // 지금 보고 있는 날/기준 날 (저장 안 함, 로드 시 오늘)
  calYear, calMonth,
  prefs: { filter, sort, hideCompleted, theme, view }
}
```

---

## 7. 엣지 케이스

- 할 일 0개 → 빈 상태 안내, 진행률 0%
- 모든 할 일 완료 → 진행률 100%, 축하 메시지(선택)
- 매우 긴 텍스트 → 줄바꿈 또는 말줄임 처리
- 필터 적용 상태에서 항목 추가 → 추가된 항목이 현재 필터와 맞지 않으면 목록에 안 보일 수 있음(진행률에는 반영). 숨겨진 개수 안내 문구 표시
- localStorage 비활성화(시크릿 모드 등) → 세션 내에서는 동작, 새로고침 시 초기화됨을 감안
- 과거 날짜를 보는 중 일별 추가 → 그 과거 날짜에 등록됨(현재 보고 있는 날짜 기준)
- 주·월 보기에서 추가 → `scope: week`/`month` 계획으로 등록, composer 아래 안내 표시
- 주 계획을 만든 뒤 일별로 내려가면 안 보임(의도됨) — 그 주로 돌아오면 다시 보임
- 달력에서 다른 달을 넘겨봐도 선택 날짜는 유지 (미리보기). 단 **월별 보기에서는** 달력 `‹ ›`가 선택 달을 옮긴다
- 주·월 보기에서 그 기간에 할 일이 하나도 없으면 "선택한 기간에 할 일이 없습니다"
- 주·월 통합 목록에서 수동 재정렬은 날짜가 달라도 가능(위치만 바뀜). 단 계획↔일반 경계는 안 넘음
- 연말/연초 경계 주의 WK 번호: 그 주 일요일이 전년이어도 날짜의 실제 연도 기준으로 계산(1/1 포함 주 = 1)
- 세부분류를 이름변경/삭제해도 기존 항목의 `subcategory`는 안 바뀜 → 그 항목은 "미분류"로 읽힘
- 여러 날 항목: `endDate ≤ date`가 되면(날짜를 뒤로 당기면) `endDate` 자동 제거
- 완료 해제 시 `doneAt` 제거(히스토리에서 사라짐). `status`와 `completed`가 모순이면 `completed` 우선
- 반복: 열린 시리즈는 앱 로드마다 8주 앞까지 채워짐. `until` 지난 시리즈는 연장 안 함.
  반복 항목 삭제는 "이 항목만 / 이 날짜 이후 모두" 선택
- `doneAt` 없는 옛 완료 항목: 히스토리에서 `createdAt`을 완료 시각으로 간주

---

## 8. 확장 기능

> 전체 버전별 변경 이력은 [`CHANGELOG.md`](CHANGELOG.md)에 있다.
> 앱 버전은 `src/version.js`(= `package.json`)에서 관리하고 제목·탭·내보내기에 표시된다.

### v4.0에서 구현

- ✅ **전체 폭 3단 레이아웃** — 분류 트리 / 달력·메모 / 상세. ≤1180px 슬라이드오버, ≤820px 스택.
- ✅ **사용자 관리형 세부분류** — 카테고리별 목록(`task-app.subcategories`), "세부분류 편집" 창.
  좌측 트리에서 카테고리/세부분류/미분류 클릭으로 필터.
- ✅ **작업중 상태** — 대기/작업중(옅은 회색)/완료. "진행" 토글 + 완료 체크박스.
- ✅ **우선순위** — 높음(빨강선+굵게+▲)/보통/낮음(▼·흐리게). `정렬: 우선순위순`.
- ✅ **상세메모** — 항목별 자동 저장 입력 칸. 편집 카드에도 포함.
- ✅ **여러 날 항목** — 편집 카드 종료일. "N일차" / "9/8–9/10" 뱃지, 달력 각 날 표시.
- ✅ **옮기기** — 편집 카드의 날짜·기간단위로 다른 날/주/월 이동. 주별 계획 → 특정 날 할당.
- ✅ **반복** — 일별에 매일/평일/매주. 인스턴스 materialize + 8주 자동 연장. 🔁 뱃지.
- ✅ **완료 히스토리** — 완료일별·최근순, 소요 시간·메모, 검색 + 완료일 기간 필터.
- ✅ **실행 방식 전환** — `.cmd` 앱모드 폐기. `index.html` 직접 + `explorer.exe` 대상 전용 아이콘 `.lnk`.

### v3.3에서 구현

- ✅ **버전 표시 + 변경 이력**: 제목/탭에 `AK Task Management v3.3.0`, `CHANGELOG.md` 신설

- ✅ **필터 칩 개수**: `전체(N) 개인(N) …` — 현재 보기 기준, 완료 숨김·필터와 무관
- ✅ **주·월별 통합 목록**: 날짜별 그룹(아젠다) 제거. 기간 전체를 한 목록으로,
  행마다 날짜 뱃지. **날짜순** 정렬 모드 추가
- ✅ **주간/월간 계획**(`Task.scope`): 주별·월별 보기에서 추가하면 특정 날이 아닌
  그 주/달의 계획으로 등록. 통합 목록 맨 위에 `WK37`(남색)·`9월`(분홍) 뱃지로 표시
- ✅ **달력 상호작용 정리**: WK 열 보라색 강조, 주별 = WK 열만 클릭·그 주 전체 밴딩,
  월별 = 그 달 전체 밴딩

### v3.2에서 구현

- ✅ **메모 스티커**: 좌측 패널, 자동 저장(`task-app.memo`)
- ✅ **리네이밍**: "할 일 관리" → **AK Task Management** (제목·문서 타이틀)
- ✅ **전용 창 배포본**: `npm run package` → Edge/Chrome `--app` 모드 실행 `.cmd`/`.lnk`,
  전용 아이콘(`app-icon.ico` + 파비콘), 전용 프로필로 창 크기·데이터 유지

### v3.1에서 구현

- ✅ **구글 캘린더식 2-패널**: 좌 달력 / 우 상세. ≤880px는 스택
- ✅ **주 번호(WK)** 열: 일요일 기준 연중 주차. 주별 보기 라벨에도 "WK37 · …"
- ✅ **월별 보기**: 진행률·기간 이동에 **월(month)** 추가
- ~~날짜별 그룹(아젠다)~~ → v3.2에서 기간 통합 단일 목록으로 대체

### v3.0에서 구현

- ✅ **월별 달력**: 메인 화면, 일요일 시작. 오늘 강조, 할 일 있는 날 점 표시(모두 완료 시 초록).
  날짜 클릭 → 이동. `‹ ›`로 다른 달 미리보기
- ✅ **일별 / 주별 보기 전환**: 기본 일별. 주별은 그 주(일~토). 설정은 저장됨
- ✅ **기간 이동 + 히스토리**: `‹ ›` + "오늘" 버튼. 지난 날짜 열람 가능
- ✅ **날짜별 배치**: 각 할 일이 `date`에 속함. 새 할 일은 현재 보고 있는 날짜에 등록
- ✅ 진행률을 **현재 보기 기준**으로 계산

### v2에서 구현

- ✅ **정렬**: 수동(드래그) / 최신순 / 오래된순 / 카테고리순 / 미완료 우선. 선택은 저장됨
- ✅ **드래그 앤 드롭 순서 변경**: 마우스 드래그 + 키보드(`↑`/`↓`). 정렬이 "수동"일 때만
- ✅ **완료 항목 숨기기** 토글. 진행률에는 계속 반영
- ✅ **데이터 내보내기/가져오기**(JSON 파일). 가져오기는 교체/병합 선택, 항목별 스키마 검증
- ✅ **테마**: 자동(시스템) / 밝게 / 어둡게
- ✅ **자동 분류**: 입력 내용의 키워드로 카테고리(개인/업무/공부)를 추측해 드롭다운을
  맞춤. 규칙 기반(오프라인), 애매하면 변경 안 함, 사용자가 직접 고르면 그 선택을 존중
- ✅ **분류 학습 + 키워드 설정**: 카테고리를 직접 교정하면 그 내용에서 키워드를 학습해
  `task-app.classifier`에 저장, 이후 분류에 반영(되돌리기 제공). `분류 설정` 다이얼로그에서
  카테고리별 키워드를 직접 추가·삭제, 내장 키워드 열람, 전체 초기화

### 아직 범위 밖

- 알림/푸시, 미완료 할 일의 자동 다음날 이월
- 반복 일정의 "이후 모두" 편집(현재는 삭제만 "이후 모두" 지원, 편집은 인스턴스 단위)
- 캘린더 격자에 항목을 드래그로 배치/이동 (편집 카드로만 재배치)
- 사용자 정의 **최상위 카테고리** 추가·삭제 (3종 고정 유지 — 세부분류는 v4에서 사용자 관리)

---

## 9. 완료 기준 (Acceptance Criteria)

- [x] 할 일을 추가/수정/삭제할 수 있다
- [x] 완료 체크를 토글할 수 있고 시각적으로 구분된다
- [x] 항목마다 개인/업무/공부 카테고리가 지정되고 색으로 구분된다
- [x] 완료/전체 비율이 숫자와 진행 바로 표시된다 (현재 보기 기준)
- [x] 새로고침·브라우저 재시작 후에도 데이터가 유지된다
- [x] 런타임 의존성·프레임워크 없이 단일 파일(`dist/index.html`)로 실행된다
- [x] 전체 폭 3단(분류 트리 / 달력·메모 / 상세)으로 표시된다 (좁은 화면은 슬라이드오버·스택)
- [x] 좌측 트리에서 카테고리·세부분류·미분류를 클릭해 목록을 필터할 수 있다
- [x] "세부분류 편집"에서 카테고리별 세부분류를 추가·이름변경·삭제할 수 있다
- [x] 할 일을 일별/주별/월별로 나눠 보고, 지난 기간으로 이동해 볼 수 있다
- [x] 주별/월별 보기에서 추가하면 그 주/달의 계획으로 등록되고, 좁은 보기에는 노출되지 않는다
- [x] 항목을 대기/작업중/완료로 둘 수 있고, 작업중은 옅은 회색 배경으로 구분된다
- [x] 항목에 우선순위(높음/보통/낮음)를 지정하고, 높음은 강조되어 보인다
- [x] 항목마다 상세메모를 optional로 적을 수 있고 자동 저장된다
- [x] 하루짜리·여러 날 항목을 만들 수 있고, 여러 날 항목은 각 날에 표시된다
- [x] 일별 항목을 다른 날/주/월로, 주별 항목을 특정 날로 옮길 수 있다
- [x] 일별 항목을 매일/평일/매주로 반복시킬 수 있다
- [x] "히스토리"에서 완료 항목을 검색·기간으로 조회하고 언제·어떻게 끝났는지 볼 수 있다
- [x] 기존 사용자 데이터(v3 Task, 새 필드 없음)가 깨지지 않고 자동 보정된다
- [x] 좌측 패널에 메모를 적으면 자동 저장되고 재실행 후에도 남아 있다
- [x] 앱 제목·탭에 "AK Task Management v4.0.0"이 표시된다
- [x] `npm run package` 배포본의 `index.html` 을 더블클릭해 열고, 전용 아이콘 바로가기를
      작업 표시줄에 고정할 수 있다
      (개발용 빌드 도구 Vite/Vitest/ESLint/Playwright는 도입 — 브라우저로는 아무것도 나가지 않음)
