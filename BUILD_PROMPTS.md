# 할 일 관리 앱 - 클로드 코드 단계별 프롬프트

> **참고 (기록용 문서).** 이 문서는 v1을 만든 최초 5단계 빌드 순서다. v2에서
> UI를 전면 개편하고 코드를 `src/` ES 모듈로 나눴으며 정렬·드래그 앤 드롭·
> 내보내기/가져오기·완료 숨기기를 추가했다. 현재 구조·제약은 `CLAUDE.md`,
> 기능·완료 기준은 `PRD.md`를 본다. 아래 내용은 갱신하지 않는다.

`PRD.md` 기반. 각 단계를 순서대로 클로드 코드에 그대로 붙여넣어 사용한다.
한 단계가 끝나고 "완료 확인" 항목을 직접 점검한 뒤 다음 단계로 넘어간다.

- 작업 디렉터리: `C:\AK_Claude\My_Task_App`
- 스택(v1): 순수 HTML + CSS + Vanilla JS, 빌드 도구·라이브러리 없음
- 파일 구성(v1): `index.html`, `style.css`, `app.js`

---

## 1단계 - 프로젝트 골격 + 데이터 레이어

```
PRD.md를 참고해서 할 일 관리 앱의 뼈대를 만들어줘.

파일 3개를 생성한다:
- index.html: 시맨틱 마크업. 헤더(앱 제목), 진행률 영역, 입력 폼 영역,
  필터 영역, 할 일 목록(<ul id="task-list">) 자리만 잡아둔다. style.css와 app.js를 연결한다.
- style.css: 기본 리셋, 시스템 폰트, 중앙 정렬된 max-width 640px 컨테이너,
  라이트 배경. 지금은 레이아웃 뼈대만.
- app.js: 아래 데이터 레이어를 구현한다.

데이터 레이어 요구사항:
- Task 객체 형태: { id, text, category, completed, createdAt }
  - id는 crypto.randomUUID(), createdAt은 Date.now()
  - category는 "개인" | "업무" | "공부"
- localStorage 키: "task-app.tasks"
- loadTasks(): localStorage에서 읽어 배열 반환. 없거나 JSON 파싱 실패 시 빈 배열 반환(앱이 깨지지 않게 try/catch).
- saveTasks(tasks): 배열을 JSON으로 직렬화해 저장.
- 런타임 상태: state = { tasks: [], filter: "전체" }
- 앱 시작 시 loadTasks()로 state.tasks를 채우고, 콘솔에 개수를 출력한다.

아직 UI 렌더링/이벤트는 만들지 않는다. 이 단계는 파일 구조와 저장/복원만.
```

**완료 확인**
- [ ] `index.html`을 브라우저로 열면 콘솔 에러가 없다
- [ ] 콘솔에서 `saveTasks([{id:'1',text:'테스트',category:'개인',completed:false,createdAt:Date.now()}])` 실행 후 새로고침하면 `loadTasks()`가 그 항목을 반환한다
- [ ] localStorage를 비운 상태에서도 에러 없이 빈 배열로 시작한다

---

## 2단계 - 할 일 추가 + 목록 렌더링

```
1단계에서 만든 앱에 할 일 추가와 목록 렌더링 기능을 붙인다.

입력 폼:
- 텍스트 input + 카테고리 select(개인/업무/공부) + "추가" 버튼
- form submit(버튼 클릭 또는 Enter)으로 추가
- text를 trim한 결과가 빈 문자열이면 추가하지 않는다(경고 불필요, 무시)
- 추가 시: 새 Task를 만들어 state.tasks 배열 맨 앞에 넣고, saveTasks() 호출 후 다시 렌더링
- 추가 후 입력창을 비우고 포커스를 되돌린다

렌더링:
- renderTasks() 함수: state.tasks를 순회하며 <li>를 그린다
- 각 <li>에 표시: 완료 체크박스(자리만, 3단계에서 동작), 할 일 텍스트, 카테고리 텍스트
- <li>에 data-id 속성으로 task.id를 넣는다
- 목록을 다시 그릴 때 기존 내용을 지우고 새로 그린다
- 항목이 0개면 "할 일이 없습니다" 안내 문구를 표시한다

상태를 바꾸는 모든 함수는 끝에 saveTasks(state.tasks)와 renderTasks()를 호출하도록 구조를 잡아라.
```

**완료 확인**
- [ ] 할 일을 입력하고 추가하면 목록 맨 위에 즉시 나타난다
- [ ] 공백만 입력하면 추가되지 않는다
- [ ] 새로고침해도 추가한 항목이 유지된다
- [ ] 전부 삭제된 상태(빈 목록)에서 안내 문구가 보인다

---

## 3단계 - 완료 체크 + 수정 + 삭제

```
각 할 일 항목에 완료 토글, 인라인 수정, 삭제 기능을 추가한다.
이벤트는 task-list에 이벤트 위임(delegation)으로 처리한다.

완료 체크:
- 체크박스 클릭 시 해당 task의 completed를 토글
- 완료 항목은 텍스트에 취소선 + 흐린 색 스타일(.completed 클래스)

삭제:
- 각 항목에 삭제 버튼(🗑 또는 "삭제"). aria-label 지정
- 클릭 시 확인 다이얼로그 없이 즉시 state.tasks에서 제거

수정:
- 각 항목에 수정 버튼(✎ 또는 "수정"), 또는 텍스트 더블클릭으로 편집 모드 진입
- 편집 모드: 텍스트가 input으로 바뀌고 카테고리 select도 함께 표시
- Enter 또는 "저장" → 변경 반영. Esc 또는 "취소" → 원복
- trim 결과가 빈 문자열이면 저장하지 않고 편집 모드 유지
- 한 번에 하나의 항목만 편집 모드가 되도록 한다

모든 변경 후 saveTasks() + renderTasks() 호출은 유지한다.
```

**완료 확인**
- [ ] 체크박스로 완료/미완료를 토글할 수 있고 취소선이 적용된다
- [ ] 삭제 버튼으로 항목이 즉시 사라진다
- [ ] 수정 모드에서 텍스트와 카테고리를 바꿔 저장하면 반영된다
- [ ] Esc로 수정을 취소하면 원래 내용이 유지된다
- [ ] 모든 변경이 새로고침 후에도 유지된다

---

## 4단계 - 카테고리 색상 구분 + 필터

```
카테고리 시각화와 필터링을 추가한다.

카테고리 뱃지:
- 각 항목에 카테고리 뱃지를 표시(색상 구분)
- 개인 / 업무 / 공부 각각 다른 색. CSS 변수로 색을 정의한다
- 뱃지는 텍스트 + 배경색, 대비 충분히

필터 UI:
- 필터 버튼 4개: 전체 / 개인 / 업무 / 공부
- 클릭 시 state.filter를 바꾸고 renderTasks() 호출
- 현재 선택된 필터 버튼에 활성 스타일(.active)
- renderTasks()는 state.filter가 "전체"가 아니면 해당 카테고리만 그린다

주의:
- 필터가 걸린 상태에서 항목을 추가했는데 그 항목이 현재 필터와 안 맞으면
  목록에 안 보인다. 이때 목록 위에 "다른 카테고리에 N개의 할 일이 더 있습니다" 같은
  안내를 표시한다.
- 필터를 바꿔도 편집 중이던 항목이 있으면 편집 상태는 취소한다.
```

**완료 확인**
- [ ] 세 카테고리가 서로 다른 색 뱃지로 구분된다
- [ ] 필터 버튼으로 특정 카테고리만 볼 수 있다
- [ ] 활성 필터 버튼이 시각적으로 구분된다
- [ ] 필터로 가려진 항목이 있을 때 안내 문구가 나온다

---

## 5단계 - 진행률 + 빈 상태/엣지 케이스 + 마무리

```
진행률 표시와 마감 처리를 한다.

진행률:
- 항상 전체 기준으로 계산(필터 무관): 완료 수 / 전체 수
- 표시 형태: "7 / 15 완료 (47%)" 텍스트 + 진행 바(<progress> 또는 div 기반)
- 상태가 바뀔 때마다 갱신
- 항목 0개: "할 일이 없습니다", 진행 바 0%
- 전체 완료(100%): 축하 문구 표시("모두 완료했어요 🎉")

마무리 점검 및 개선:
- 매우 긴 할 일 텍스트가 레이아웃을 깨지 않도록 word-break/overflow 처리
- 모바일 폭(375px)에서 목록과 버튼이 세로로 정상 배치되는지 확인하고 반응형 보정
- 버튼에 aria-label, 체크박스에 연결된 label, 키보드로 모든 조작 가능하게
- localStorage가 막힌 환경(시크릿 모드)에서 저장 실패해도 앱이 죽지 않도록 saveTasks를 try/catch로 감싼다
- README.md 작성: 앱 소개, 실행 방법(index.html 더블클릭), 파일 구조, 데이터 저장 방식

마지막으로 PRD.md의 "완료 기준" 체크리스트 항목을 하나씩 검증하고 결과를 보고해줘.
```

**완료 확인**
- [ ] 진행률이 숫자와 바로 정확히 표시되고 실시간 갱신된다
- [ ] 빈 목록 / 전체 완료 상태의 문구가 올바르게 나온다
- [ ] 모바일 폭에서 레이아웃이 깨지지 않는다
- [ ] 키보드만으로 추가/체크/수정/삭제/필터가 가능하다
- [ ] `README.md`가 있고 PRD 완료 기준 7개 항목이 모두 충족된다

---

## 진행 순서 요약

| 단계 | 산출물 | 핵심 |
|------|--------|------|
| 1 | 파일 3개 + 데이터 레이어 | localStorage 저장/복원, Task 모델 |
| 2 | 추가 + 렌더링 | 입력 폼, renderTasks(), 빈 상태 |
| 3 | 체크 + 수정 + 삭제 | 이벤트 위임, 인라인 편집 |
| 4 | 카테고리 + 필터 | 색상 뱃지, state.filter |
| 5 | 진행률 + 마무리 | 진행 바, 엣지 케이스, 접근성, README |
