# 변경 이력 (Changelog)

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을,
버전은 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따른다.

- 표시 버전은 앱 상단 제목과 브라우저 탭에 `AK Task Management v4.1.0` 형태로 나온다
  (`src/version.js`의 `VERSION` — `package.json`의 `version`과 항상 일치, `tests/version.test.js`가 검사).
- 내보내기(JSON) 파일에도 `appVersion` 필드로 기록된다.

---

## [4.5.1] — 2026-09-17

### 추가

- **할 일 카운터 기능**: 우측 할 일 목록의 툴바에 현재 표시 중인 할 일 총 개수("총 N개")를 실시간으로 집계 및 표시하는 기능 도입
- **세부분류 위/아래(▲, ▼) 버튼 순서 이동**: 통합설정의 세부분류 관리에서 기존 드래그 방식을 대체하여, 시인성이 높고 간편하게 위아래 화살표 버튼으로 항목 순서를 한 칸씩 조율할 수 있도록 개편
- **환경설정 내보내기/가져오기 확장**: 수동/자동 백업 파일 생성 시 할 일 목록 외에도 사용자의 자동 백업 환경설정(BACKUP_CONFIG)을 포함하고, "교체 가져오기" 시 이 값들을 완벽하게 자동 복원하도록 연동

### 수정

- **v4.3 백업 데이터 유입 호환성 조치**: 이전 백업 가져오기 시 세부분류 필드 유실로 인한 로컬 스토리지 전체 초기화 현상 및 새로고침 시 데이터 소멸 크래시 현상 완전 해결
- **세부분류 병합(Merge) 시 TypeError 해결**: 가져오기 병합 선택 시 업무 카테고리의 중첩 객체(Map)를 스프레드로 전개하여 발생하던 브라우저 TypeError 크래시 조치
- **자동 백업 설정 저장 복구**: "요일 선택" 필드가 매일 백업 방식으로 변경됨에 따라 발생하던 `daySelect` 미정의 ReferenceError 해소 및 저장 로직 복원

---

## [4.5.0] — 2026-09-16

### 추가

- **시간별 보기 고도화**:
  - 메모 기능: 시간별 보기에서 메모 추가/편집 (일별과 동일한 자동저장)
  - 카테고리 칩: 우측에 카테고리별 구분 표시
  - 메모 토글: "메모•" 버튼으로 메모 펼침/접음
  - 1시간 단위 시간표: 08:00-20:00 범위
  - 균등 높이: 모든 시간 슬롯을 150px로 통일 (메모 포함)
  - 명확한 구분줄: 시간과 시간 사이 진한 테두리

### 수정

- **메모 저장 개선**: 모든 메모(좌측 노란색 메모, 할일 메모)는 입력 후 500ms 내 자동저장 (저장 버튼 제거)
- **자동백업 주기 변경**: 주 1회 → **매일** (시간 설정 가능, 요일 선택 제거)
- **편집창 UI 개선**:
  - 주별/월별 계획 선택 시 "종료일(TO)"과 "시간할당" 필드 비활성화
  - 비활성화된 필드는 옅은 회색(#E8E8E8) 표시
  - 커서를 "not-allowed"로 변경

### 보존

- 세부분류 원래 색상 복구 (청록색: light #ccfbf1, dark #134e4a)

## [4.4.0] — 2026-09-16

### 추가

- **백업 완전성 개선**: 내보내기/가져오기/자동백업 시 메모와 세부분류 항목 포함
  - 이전: 할일 목록만 백업/복원
  - 이제: 메모 + 세부분류 + 할일 모두 함께 저장/복원

### 수정

- **세부분류 필터링 개선**: 부모 세부분류(예: 빅데이터) 클릭 시 하위 항목들의 모든 할일 표시
  - 부모 항목(빅데이터) → 로케이션찾기, 이슈분석 할일 모두 표시
  - 자식 항목(로케이션찾기) 클릭 → 그 항목만 표시
  - 부모/자식 형태의 subcategory("빅데이터/로케이션찾기") 지원
- **필터 카운트 정확성**: 현재 필터에 맞는 항목만 "숨겨진 할일" 카운트 (다른 카테고리 제외)

## [4.3.0] — 2026-09-14

### 변경

- **앱 이름 단순화** — "AK Task Management" → "Task Management"
- **좌측 패널 스크롤 개선**
  - 좌측 패널(일정관리 + 업무현황)을 `position: sticky`로 화면 고정
  - 달력 패널처럼 완전히 고정되며 내부만 스크롤 가능
  - 우측 할 일이 많아져도 좌측 패널은 화면에 유지

## [4.2.0] — 2026-09-13

### 변경

- **좌측 패널 레이아웃 최적화**
  - 일정관리 트리 패널 70% 확대, 업무현황 패널 30% 축소
  - 달력 패널과 좌측 패널 높이 완벽 정렬
- **주별 업무 분석 개선**
  - 좌측 아래 고정 패널로 이동 (기존: 독립적 위치)
  - 달력 선택과 자동 동기화 — weekOffset 제거
  - 현재 주 기준으로 실시간 업무 분포 표시
- **세부분류 구조 확인** — 모든 카테고리(개인/공부/업무)에서 세부분류 지원

### 수정

- `parseFilter` 반환값 필드명 통일 (`subpath` → `sub`)
- 트리 뷰 expandedNodes 로직 안정화

## [4.1.0] — 2026-09-13

### 변경

- **UI/UX 디자인 시스템 개선**
  - 색상 팔레트 업데이트: Primary Indigo (#4f46e5 → #6366f1), Success Green (#16a34a → #22c55e)
  - 배경색 따뜻한 톤 조정 (#f6f7f9 → #fafbfc)
  - Warm Orange accent (#f97316) 추가
- **타입페이스 개선** — Noto Sans KR 추가로 한글 가독성 향상
- **컴포넌트 스타일** — border-radius 12px, 여백 조정, 그림자 효과 개선

## [4.0.0] — 2026-09-08

대규모 개편. 기존 사용자 데이터(v3 Task)는 그대로 호환된다.
세부분류 · 작업중 상태 · 우선순위 · 상세메모 · 여러 날 · 반복 · 완료 히스토리,
전체 폭 3단 레이아웃, 실행 방식 전환.

### 데이터 레이어 (P1)

- **Task 스키마 확장** — 모두 선택 필드, 기본값이면 저장하지 않음:
  `status`("doing"), `priority`("high"/"low"), `subcategory`, `endDate`(여러 날),
  `notes`(상세메모), `doneAt`(완료 시각), `recurrence`/`series`(반복).
  `completed`는 done의 정본으로 유지(frozen).
- **`task-app.subcategories`** 저장키 신설 — 카테고리별 세부분류 목록. 첫 실행 시
  업무에 `빅데이터 · AI/자동화 · UT일반 · UT과제` 시딩.
- `tasksOnDay`/`countByDate`가 여러 날 항목을 각 날에 반영. `sortTasks`에
  **우선순위순** 추가, **진행 우선**은 작업중을 맨 앞으로. 필터 문자열이
  `"업무/빅데이터"` 형태의 세부분류 필터를 지원.
- 내보내기 JSON `version` 4 → 5.
- `src/subcats.js`(순수 세부분류 CRUD), `tasks.js`에 반복(`buildSeries`/`topUpSeries`/
  `expandRecurrence`) · 히스토리(`historyEntries`) 로직 추가.

### 화면 / 상호작용 (P2)

- **전체 폭 3단 레이아웃** — `.nav-pane`(분류 트리) | `.cal-pane`(달력·메모) | `.detail-pane`.
  1040px 고정폭 제거. ≤1180px는 트리를 "☰ 분류" 슬라이드오버로, ≤820px는 세로 스택.
- **좌측 분류 트리** — 전체 / 카테고리 / 세부분류 / 미분류 행, 현재 보기 기준 개수.
  클릭 → 목록 필터(`prefs.filter` = `"업무/빅데이터"` 형태). 상단 칩 행은 제거.
- **세부분류 편집** 다이얼로그 — 카테고리별 추가·이름변경·삭제.

### 항목 관리 (P3)

- **작업중 상태** — 완료 외에 "진행" 토글. 작업중 항목은 옅은 회색 배경.
- **우선순위** — 높음/보통/낮음. 높음은 좌측 빨강선 + 굵게 + ▲, 낮음은 흐리게 + ▼.
  `정렬: 우선순위순` 추가.
- **상세메모** — 항목마다 "메모" 토글로 펼치는 입력 칸(자동 저장). 편집 카드에도 포함.
- 편집이 인라인 행 → 2행 편집 카드(내용 / 카테고리·세부분류·우선순위 / 메모).

### 일정 조정 (P4)

- **여러 날 항목** — 편집 카드에서 종료일 지정. 달력·목록에 각 날 표시,
  일별 보기 행에 "N일차" 뱃지, 주·월 보기 행에 "9/8–9/10" 범위 뱃지.
- **옮기기** — 편집 카드에 날짜·종료일·기간 단위(일/주/월) 필드. 다른 날로 이동,
  주별 계획을 특정 날 일별 항목으로 전환.

### 반복 (P5)

- **반복 일정** — 일별 항목에 매일 / 평일 / 매주(요일 선택). 실제 인스턴스를 8주치
  만들고, 앱을 열 때마다 8주 앞까지 자동 연장. 반복 항목은 🔁 뱃지.
- 반복 항목 삭제 시 "이 항목만 / 이 날짜 이후 모두" 선택.

### 히스토리 (P6)

- **완료 히스토리** — 앱 바 "히스토리" 버튼. 완료된 항목을 완료일별로 묶어
  최근순 표시(완료 시각, 소요 시간, 세부분류, 메모). 내용 검색 + 완료일 기간 필터.
  `src/history.js` (다이얼로그) + `historyEntries()` (tasks.js).

### 다듬기

- 좌측 트리에서 세부분류를 클릭하면 입력창의 카테고리·세부분류에도 반영된다
  ("빅데이터" 클릭 → 그대로 "추가"하면 `[빅데이터] …`로 저장). 이름에 `/`가 든
  세부분류(예: `AI/자동화`)도 정상 동작(첫 `/`만 기준으로 분리).
- 세부분류 칩을 내용 **앞**에 태그로, **청록색**으로 표시(회색 "진행" 토글과 구분).
- 좌측 메모를 기본으로 창 높이 근처까지 크게(달력 패널이 화면 높이를 차지하고 메모가 남은
  세로 공간을 채움). 좁은 화면에서는 보통 크기.

### 실행 방식 (P7)

- **`.cmd` 앱모드 런처 폐기.** `index.html` 을 더블클릭하면 기본 브라우저에서
  바로 열린다(일반 탭). 작업 표시줄 고정용 `.lnk` 는 대상이 **`explorer.exe`**
  (진짜 실행 파일 → "작업 표시줄에 고정" 메뉴가 뜨고, 브라우저 종류에 의존하지 않음).
  전용 브라우저 프로필·고정 창 크기는 없어졌다.
- 배포본 구성 정리: `index.html`, `app-icon.ico`, `AK Task Management.lnk`,
  `바로가기 만들기.cmd`, `_shortcut.ps1`, `변경이력.txt`, `사용법.txt`.

## [3.3.0] — 2026-09-07

### 추가

- **주간 / 월간 계획** (`Task.scope`) — 주별·월별 보기에서 할 일을 추가하면 특정 날이 아니라
  **그 주 / 그 달 전체의 계획**으로 등록된다. 통합 목록 맨 위에 모이고
  `WK37`(남색) · `9월`(분홍) 뱃지로 구분 표시. 일별 보기에는 노출되지 않는다.
  (`scope`는 `week` / `month`일 때만 저장 — 없으면 `day`, 예전 데이터와 호환)
- **필터 칩 개수** — `전체(N) 개인(N) 업무(N) 공부(N)`. 현재 보기 기준이며 완료 숨김·필터와 무관.
- **정렬: 날짜순** 모드 추가.
- 앱 제목 · 브라우저 탭에 **버전 표시**, 이 변경 이력 파일(`CHANGELOG.md`).

### 변경

- **주별 · 월별 = 통합 목록** — 날짜별 그룹(아젠다) 제거. 기간 전체를 하나의 목록으로 관리하고,
  행마다 날짜 뱃지(`오늘` / `9/8 (화)`)를 붙인다.
- **달력 상호작용** — WK 열 숫자를 보라색으로 강조. 주별 보기에서는 **WK 열만 클릭 가능**(그 주 선택),
  개별 날짜 셀은 클릭 불가. 월별 보기는 표시 중인 달 전체를 파란 배경으로 표시.
- 주 · 월 통합 목록의 수동 재정렬이 날짜 경계를 넘을 수 있다(계획 ↔ 일반 항목 경계는 유지).
- 내보내기 JSON `version` 3 → 4 (`scope` 필드 추가에 따른 스키마 버전).

## [3.2.0] — 2026-09-07

### 추가

- **메모 스티커** — 좌측 달력 아래 자유 기록. 입력하면 자동 저장(`task-app.memo`).
- **전용 창 배포본** — `npm run package` → `release/AK Task Management/`
  (단일 파일 앱 + Edge/Chrome `--app` 모드 실행 `.cmd`/`.lnk` + `바로가기 만들기.cmd` + `사용법.txt`) + zip.
  전용 브라우저 프로필로 창 크기·위치·데이터 유지.
- **전용 아이콘** — `scripts/make-icon.mjs`(순수 Node)가 `app-icon.ico`(16–256px)와
  `index.html` 파비콘(64px PNG data URI)을 생성. 앱 창·작업표시줄 아이콘이 기본 지구본이 아닌 체크 마크.

### 변경

- 앱 이름: **"할 일 관리" → "AK Task Management"** (제목 · 문서 타이틀 · 패키지명).

## [3.1.0] — 2026-09-07

### 추가

- **구글 캘린더식 2-패널 레이아웃** — 좌측 달력(내비게이터) / 우측 선택 기간 상세. ≤880px 세로 스택.
- **주 번호(WK) 열** — 일요일 기준 연중 주차(1/1 포함 주 = 1주차). 주별 보기 라벨에도 "WK37 · …".
- **월별 보기** — 진행률 · 기간 이동에 월(month) 단위 추가.

## [3.0.0] — 2026-09-07

### 추가

- **`Task.date`** (`"YYYY-MM-DD"`) 필드 — 할 일을 날짜에 배치. 구 데이터는 로드 시 `createdAt`에서 자동 보정.
- **월별 달력** — 메인 화면, 일요일 시작. 오늘 강조, 할 일 있는 날 점 표시(모두 완료 시 초록).
- **일별 / 주별 보기 전환** + 기간 이동(`‹ ›`, "오늘") · 지난 기간 히스토리 열람.
- **진행률을 현재 보기 기준**으로 계산.

## [2.0.0] — 2026-09-07

### 추가

- 빌드/개발 도구 도입 — Vite + Vitest + ESLint + Prettier, `src/` ES 모듈 구조,
  `vite-plugin-singlefile`로 단일 `dist/index.html` 유지.
- **인라인 편집**, **카테고리 색상 뱃지 + 필터**.
- **정렬** — 수동(드래그) / 최신순 / 오래된순 / 카테고리순 / 미완료 우선.
- **드래그 앤 드롭 순서 변경** (마우스 + 드래그 핸들 방향키).
- **완료 항목 숨기기** 토글.
- **자동 분류** — 입력 키워드로 카테고리 추측(규칙 기반, 오프라인) + 분류 학습 · 키워드 설정 창.
- **내보내기 / 가져오기** (JSON, 교체 또는 병합).
- **테마** — 자동(시스템) / 밝게 / 어둡게.

## [1.0.0]

### 추가

- 최초 버전 — 할 일 추가 · 수정 · 삭제, 완료 체크, 카테고리(개인/업무/공부), 진행률,
  `localStorage` 저장. 순수 자바스크립트, 브라우저 단일 파일로 실행.
